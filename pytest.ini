[pytest]
asyncio_mode = auto
markers =
    postgres_integration: requires PostgreSQL (skips if unreachable; see docs/local_postgres_testing.md)
    command_handler: command handler tests (SpyStore; see ledger/domain/commands/README.md)
