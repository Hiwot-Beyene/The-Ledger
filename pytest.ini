[pytest]
pythonpath = src
asyncio_mode = auto
markers =
    postgres_integration: requires PostgreSQL (skips if unreachable; see README.md)
    command_handler: command handler tests (SpyStore; see src/commands/README.md)
