# Optional. If unset during `npm run dev`, /api is proxied to http://127.0.0.1:8080 (see vite.config.ts).
# Set explicitly when the API is on another host or for production builds.
#VITE_API_URL=http://localhost:8080
